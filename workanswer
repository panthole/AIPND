任务1. 好友男女比例

# TODO：统计好友性别
male = [data for data in dataset if data['Sex']==1]
female = [data for data in dataset if data['Sex']==2]
sex = {
    'male': len(male),
    'female': len(female),
    'unknown': len(dataset)-len(male)-len(female)
}

任务2. 好友地域分布

### TODO：将好友所在省份（不为空）添加到 province 中
province = []
for data in dataset:
    if data['Province']:
        province.append(data['Province'])

任务3. 生成好友个性签名词云图

### TODO：打印出第2条签名
print(dataset[1]['Signature'])

任务4. 对好友签名进行情感分析

### TODO：统计sentiments中大于0.66的个数
positive = len([ sentiment for sentiment in sentiments if sentiment > 0.66])

### TODO：统计sentiments中大于等于0.33且小于等于0.66的个数
neutral = len([sentiment for sentiment in sentiments if sentiment >=0.33 if sentiment <=0.66])

### TODO：统计sentiments中小于0.33的个数
negative = len([sentiment for sentiment in sentiments if sentiment < 0.33])
